 db.createCollection('premium_tools', { validator: { $jsonSchema: { bsonType: 'object', required: ['service_name', 'service_desc', 'service_url', 'cookies'], properties: { service_name: { bsonType: 'string', description: 'Must be a string and is required' }, service_desc: { bsonType: 'string', description: 'Must be a string and is required' }, service_url: { bsonType: 'string', description: 'Must be a string and is required' }, cookies: { bsonType: 'array', description: 'Must be an array and is required' } } } } });
 db.premium_tools.createIndex({ service_name: 1 }, { unique: true })
